{% extends 'base.html.twig' %}
{% block title %} Billing Address{% endblock %}
{% block body %}
    <section class="section-checkout-page section-shopping-cart-page">
        <div class="container">
            <div class="row">
                <div class="col-md-12 col-lg-9">
                    <div class="panel-group checkout-accordions">
                        <div class="panel">

                            <!-- Checkout progress -->
                            <div id="checkout-progress">
                                <ul class="breadcrumb list-inline checkout-progress">
                                    <li><a class="progress-active">BILLING ADDRESS</a></li>
                                    <li>SHIPPING ADDRESS</li>
                                    <li>SHIPPING METHOD</li>
                                    <li>PAY</li>
                                </ul>
                            </div>
                            <div id="checkout-page">
                                <!-- End id="checkout-progress" -->
                                <div class="panel-heading">
                                    <h4 class="panel-title">
                                        <span>billing address</span>
                                    </h4>
                                </div>
                                <div class="panel-body step-1">
                                    {{ form_start(buyerCartForm) }}
                                    {{ form_widget(buyerCartForm) }}
                                    <button type="submit" class="md-button step-btn" formnovalidate>Continue</button>
                                    {{ form_end(buyerCartForm) }}
                                </div>
                            </div>

                        </div>
                    </div>
                </div>
                <div id="cart-totals"> {{ include(':partials:checkout-totals.htm.twig') }}</div>
            </div>

        </div>
    </section>

{% endblock %}

